# WebRTC offer endpoint. Defaults to /api/offer, which the Vite dev server
# proxies to the bot on port 7860. Set an absolute URL for production
# builds served away from the bot.
# VITE_OFFER_URL=https://bot.example.com/api/offer
