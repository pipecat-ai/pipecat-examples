[project]
name = "pipecat-langfuse-tracing"
version = "0.1.0"
description = "A Pipecat example using Langfuse tracing"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[daily,webrtc,websocket,silero,cartesia,deepgram,openai,tracing,runner]>=1.8.0",
    "pipecatcloud>=1.2.0",
    "opentelemetry-exporter-otlp-proto-http",
    "aiohttp",
]

[dependency-groups]
dev = [
    "ruff~=0.12.1",
]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["I"]
