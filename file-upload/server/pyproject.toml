[project]
name = "file-upload"
version = "0.1.0"
description = "Voice AI bot built with Pipecat"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[anthropic,cartesia,daily,deepgram,runner,silero,webrtc]>=1.4.0",
    "pipecatcloud",
]

[dependency-groups]
dev = [
    "pyright>=1.1.404,<2",
    "ruff>=0.12.11,<1",
]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["I", "UP"]

# TODO: remove before merge — local override for testing against the sibling
# pipecat checkout (pipecat-ai/pipecat PR #3415) until the feature is released.
[tool.uv.sources]
pipecat-ai = { path = "../../../pipecat", editable=true }