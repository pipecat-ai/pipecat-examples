[project]
name = "speculative-user-aggregator"
version = "0.1.0"
description = "An example voice agent that uses Ink's turn.eager_end events"
requires-python = ">=3.11,<4"
dependencies = [
    "pipecat-ai[daily,webrtc,websocket,cartesia,openai,runner]>=1.8.0,<2",
    "pipecatcloud>=1.2.0,<2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.404,<2",
    "ruff>=0.12.11,<1",
]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["I"]
