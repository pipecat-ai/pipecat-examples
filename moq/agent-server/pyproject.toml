[project]
name = "moq-agent-server-example"
version = "0.1.0"
description = "Announcement-driven, multi-session MoQ voice-agent server example for Pipecat"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    # response_path / request_path and the split client-mode origins landed
    # in 1.8.0; this example needs both.
    "pipecat-ai[moq,silero,deepgram,cartesia,openai]>=1.8.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.404,<2",
    "ruff>=0.12.11,<1",
]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["I"]
